# ══════════════════════════════════════════
# دلّة – Production Environment Variables
# ══════════════════════════════════════════
# انسخ هذا الملف إلى .env محلياً للتطوير.
# على Render: هذه القيم تُضبط تلقائياً عبر render.yaml،
# عدا أسرار SMS التي يجب إضافتها يدوياً من لوحة Render.

# ── Server ────────────────────────────────
PORT=4000
NODE_ENV=development        # development | production

# ── Database (PostgreSQL) ─────────────────
# على Render: يُضبط تلقائياً عند ربط قاعدة بيانات بالخدمة (render.yaml)
DATABASE_URL="postgresql://dalla:dallalocal@localhost:5432/dalla"

# ── Auth Secrets ──────────────────────────
# ولّد قيمة عشوائية قوية محلياً بـ:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# على Render: يُولَّد تلقائياً (generateValue: true في render.yaml)
JWT_SECRET="change-me-to-a-long-random-string-min-48-chars"
JWT_EXPIRES_IN="30d"

# ── OTP ────────────────────────────────────
OTP_TTL_SECONDS=60
OTP_MAX_ATTEMPTS=3

# ── CORS ──────────────────────────────────
# عناوين الواجهة المسموح لها بالوصول، مفصولة بفواصل
ALLOWED_ORIGINS="*"
